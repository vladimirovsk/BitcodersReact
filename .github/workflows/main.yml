name: Bitcoders Front

on:
  push:
    branches: [ dev ]
  pull_request:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Login in Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Publish Regisry
        uses: minddocdev/docker-publish-action@master
        with:
          name: vladimirovsk/bitcoders-react
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          extratags: master




#      - name: build
#        run: docker build . -t vladimirovsk/bitcoders-react:master
#      - name: publish
#        run: |
#          docker login -u vladimirovsk -p ${{ secrets.DOCKER_PASSWORD }}
#          docker push vladimirovsk/bitcoders-react:master